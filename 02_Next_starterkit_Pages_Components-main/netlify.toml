[build]
  # Let op: de naam moet EXACT zijn zoals in VS Code (met de underscore én streepjes)
  base = "Reactiviteit_-UX_UI"
  
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"